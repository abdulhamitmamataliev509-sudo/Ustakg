# ==================================================
# Usta kg — Backend Python Dependencies
# FastAPI + SQLAlchemy + Alembic + PostgreSQL
# ==================================================

# Web framework
fastapi
uvicorn[standard]

# Database ORM & migrations
sqlalchemy
alembic
psycopg2-binary
# asyncpg  # <-- alternative async PostgreSQL driver

# Validation & settings
pydantic
pydantic-settings

# Auth / security
python-jose[cryptography]
passlib[bcrypt]
bcrypt==4.0.1  # pinned for passlib 1.7.4 compatibility (bcrypt 4.1+ removed __about__)
python-multipart

# Testing
pytest
httpx2  # required by starlette TestClient
